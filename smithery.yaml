# Smithery configuration: https://smithery.ai/docs/build/project-config/smithery-yaml

startCommand:
  type: stdio

deploymentUrl: https://github.com/silbercue/SilbercueSwift

configSchema:
  type: object
  required: []
  properties: {}

commandFunction: |-
  (config) => ({ command: 'SilbercueSwift', args: [], env: {} })
